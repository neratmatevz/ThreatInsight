/*
    Function reads the parameters that are json encoded and 
    recognizes the parameters important for the API call to
    the nmap API web service.

    Function should be called only from the function that
    calls processes the JSON date from the "Run" on frontend.

    @param {Object} nmapJsonData - JSON object containing parameters for the Nmap API call.
*/
const nmap = (nmapJsonData) => {
    //TODO: get nmap data from JSON
    //TODO: setup parameters for nmapAPIcall()
    //TODO: call the nmapAPIcall()
    //TODO: save the result to database 
    //TODO: return that the result is saved to database and search for this tool is over
}

export {nmap};



